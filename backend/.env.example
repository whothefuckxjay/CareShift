# Copy this file to .env and adjust as needed.

# If using the included docker-compose.yml, this matches it out of the box.
DATABASE_URL="postgresql://careshift:careshift@localhost:5432/careshift?schema=public"

# Any long random string. This is a demo/local setup — not a production secret.
JWT_SECRET="dev-only-secret-change-me"

PORT=4000
